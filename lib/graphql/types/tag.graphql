type Tag {
  id: ID!
  name: String!
  slug: String
  posts:[Post!]
}

input InpuTag{
  id:ID
  name:String
}

type Query {
  # Obtener un tag por su id
  tag(where:InpuTag):Tag
  # Obtener tags
  tags(pages:Paginacion): [Tag!]
}
type Error{
  path:String
  message:String
}

scalar IntSegurity

input Paginacion{
  limit: IntSegurity
  cursor: Int
  #order ASC o DESC
  order: [[String]!]
  #Ejecuta el metodo Dataloader.clear(keys)
  clear:[Int!]
}